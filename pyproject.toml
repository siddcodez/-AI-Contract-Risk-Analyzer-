[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-contract-risk-analyzer"
version = "0.1.0"
description = "AI-powered multi-tenant contract risk analysis platform"
requires-python = ">=3.12"
dependencies = [
    # Web framework
    "fastapi[standard]>=0.115.0",
    "uvicorn[standard]>=0.30.0",

    # Database
    "sqlalchemy[asyncio]>=2.0.36",
    "asyncpg>=0.30.0",
    "alembic>=1.14.0",

    # Config & validation
    "pydantic>=2.9.0",
    "pydantic-settings>=2.6.0",

    # Redis / Celery
    "redis[hiredis]>=5.2.0",
    "celery[redis]>=5.4.0",

    # Object storage
    "supabase>=2.10.0",

    # Document processing & embeddings (M3)
    "pgvector>=0.3.0",
    "pypdf>=5.0.0",
    "python-docx>=1.1.0",

    # Logging
    "structlog>=24.4.0",

    # HTTP client (health checks + LLM calls later)
    "httpx>=0.27.0",

    # Auth
    "bcrypt>=4.0.0",
    "PyJWT>=2.9.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=6.0.0",
    "ruff==0.16.2",
    "mypy>=1.13.0",
    "pre-commit>=4.0.0",
]

# ---------------------------------------------------------------------------
# Tool configuration
# ---------------------------------------------------------------------------

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["app", "tests", "scripts"]

[tool.ruff.format]
line-ending = "lf"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "S",   # flake8-bandit (security)
    "T20", # flake8-print (no stray print())
    "RUF", # ruff-specific rules
]
ignore = [
    "S101",  # use of assert — needed in tests
    "B008",  # function call in default arg — FastAPI Depends() pattern
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S", "T20"]
"scripts/**" = ["T20"]  # scripts may print
"scratch/**" = ["S", "T20", "E501", "B007", "I001"]


[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]
ignore_missing_imports = false
exclude = ["alembic/"]

[[tool.mypy.overrides]]
module = [
    "celery.*",
    "asyncpg.*",
    "supabase.*",
    "storage3.*",
    "postgrest.*",
    "redis.*",
    "jwt.*",
    "fitz.*",
    "docx.*",
]
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "--cov=app --cov-report=term-missing --cov-fail-under=65"
markers = [
    "integration: tests that require real backing services (PostgreSQL, Redis, etc.)",
]
filterwarnings = [
    "error",
    "ignore::DeprecationWarning",
    "ignore::PendingDeprecationWarning",
    "ignore::ResourceWarning",
    "ignore::RuntimeWarning",
    "ignore:.*Using `httpx` with `starlette.testclient`.*:Warning",
]

[tool.coverage.run]
source = ["app"]
omit = ["app/workers/celery_app.py"]  # Celery wiring is integration-tested, not unit-tested

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "@(abc\\.)?abstractmethod",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*", "scripts*"]
